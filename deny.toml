# cargo-deny configuration.
# https://embarkstudios.github.io/cargo-deny/

[graph]
all-features = true

[advisories]
version = 2
yanked = "warn"

[licenses]
version = 2
allow = [
    "Apache-2.0",
    "MIT",
    "MIT-0",
    "BSD-2-Clause",
    "BSD-3-Clause",
    "ISC",
    "Unicode-3.0",
    "CC0-1.0",
    "CDLA-Permissive-2.0",  # webpki-root-certs (cert bundle)
]
confidence-threshold = 0.93

[bans]
multiple-versions = "warn"
wildcards = "deny"
# In-workspace path deps are written without explicit versions; that's
# fine since they're not external. Without this, cargo-deny flags every
# `path = "..."` dependency as a wildcard.
allow-wildcard-paths = true
# Skip duplicates that come from major transitive dep updates; revisit
# when the ecosystem catches up.
skip-tree = [
    { name = "windows-sys" },
    { name = "windows-targets" },
    { name = "getrandom" },
]
# Specific duplicates from independent transitive chains that we can't
# unify by bumping a workspace dep. Revisit when upstream catches up.
skip = [
    # core-foundation 0.9 via system-configuration <- hyper-util <- axum.
    # core-foundation 0.10 via rustls-platform-verifier. Different chains;
    # can't unify without an upstream bump in one of them.
    { name = "core-foundation" },
]

[sources]
unknown-registry = "deny"
unknown-git = "deny"
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
